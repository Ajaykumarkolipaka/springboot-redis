package com.example.springbootredis.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class BeanConfig {
	
	/*
	 * @Bean JedisConnectionFactory getJedisConnectionFactory() { return new
	 * JedisConnectionFactory(); }
	 * 
	 * @Bean RedisTemplate<String, Employee> getRedisTemplate() {
	 * RedisTemplate<String, Employee> redisTemplate = new RedisTemplate<>();
	 * redisTemplate.setConnectionFactory(getJedisConnectionFactory()); return
	 * redisTemplate; }
	 */
}
